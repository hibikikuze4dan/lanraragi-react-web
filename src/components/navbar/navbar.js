import React from "react";
import { BottomNavbar } from "./fragments/bottom-navbar";

export const Navbar = () => (
  <>
    {/* {matches && <SideNavbar />}
      {!matches && <BottomNavbar />} */}
    <BottomNavbar />
  </>
);
